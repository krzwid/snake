package sample.food;

import javafx.scene.paint.Color;

public class SlowFood extends Food {
    public SlowFood (int positionX, int positionY) {
        super(positionX, positionY);
        setFill(Color.PURPLE);
    }
}
